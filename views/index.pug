doctype html
html(language="eng")
    include ./head.pug
    body
        
        section(class="hero")
            a(href="/" class="logo") <span>QUAD</span> CAPTCHA

            if success.length > 0
                p(class="nortification animateOpen")= success

            div(class="container")
                include ./heroContent
                include ./form

        script(src="public/quadCaptcha.js")
